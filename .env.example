# Root Environment Variables
# Copy this file to .env and configure the values

# Development Environment
NODE_ENV=development

# Docker Services
# PostgreSQL Database
POSTGRES_DB=olhapi_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Redis Cache
REDIS_PASSWORD=

# Shared Configuration
# JWT secrets and other shared secrets can be defined here
# Individual apps should have their own .env files for app-specific config

# SonarQube
# Generate token at https://sonarcloud.io/account/security
SONARQUBE_TOKEN=your_sonarqube_token_here

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Configure individual app environments:
#    - apps/api/.env (API server configuration)
#    - apps/frontend/.env (Frontend configuration)
#    - apps/promo/.env (Promo site configuration)
# 3. Start development services: pnpm dev:services
# 4. Start applications: pnpm dev